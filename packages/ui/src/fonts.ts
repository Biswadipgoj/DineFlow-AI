// Fonts are loaded at runtime via a <link> stylesheet rather than
// next/font/google, so production builds never depend on network access
// to Google Fonts. The CSS variables (--font-inter / --font-poppins) are
// defined in globals.css with system-font fallbacks.
export const GOOGLE_FONTS_URL =
  'https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@400;500;600;700&display=swap';
